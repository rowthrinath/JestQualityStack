const sum = require('./qualitystack.test.js');

test('Launch quality stack website', () => {
  const component = renderer.create(
    <Link page="http://localhost:3000/">Qiuality Stack</Link>,
  );
});